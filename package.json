{
  "name": "testJoule",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@sap-cloud-sdk/http-client": "^4",
    "@sap-cloud-sdk/resilience": "^4",
    "@sap/cds": "^9",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "hybrid": "npx cds bind --exec npx cds serve --profile hybrid",
    "build": "cds build --production",
    "watch": "cds watch"
  },
  "private": true,
  "cds": {
    "requires": {
      "OnPremService": {
        "kind": "odata",
        "credentials": {
          "destination": "S4HANA_PCE_SSO"
        }
      },
      "[production]": {
        "auth": "xsuaa"
      },
      "connectivity": true,
      "destinations": true
    }
  }
}
